import { type FormEvent, useEffect, useState } from 'react';
import { useTranslation } from 'react-i18next';
import { cartaoCreditoApi } from '../lib/cartao-credito-api.ts';
import { bandeiraCartaoApi } from '../lib/bandeira-cartao-api.ts';
import type { CartaoCredito, CartaoCreditoInput } from '../types/cartao-credito.ts';
import type { BandeiraCartao } from '../types/bandeira-cartao.ts';
import { useCrudResource } from '../hooks/use-crud-resource.ts';
import { SimpleCrudPage } from '../components/simple-crud-page.tsx';
import { Input } from '../components/ui/input.tsx';
import { Label } from '../components/ui/label.tsx';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '../components/ui/select.tsx';
const sortItems = (items: CartaoCredito[]) => [...items].sort((a, b) => a.name.localeCompare(b.name));
const dueDays = Array.from({ length: 31 }, (_, index) => index + 1);
export function CreditCards() { const { t } = useTranslation(); const [brands, setBrands] = useState<BandeiraCartao[]>([]); const [brandsError, setBrandsError] = useState(''); const [name, setName] = useState(''); const [cardBrandId, setCardBrandId] = useState(''); const [dueDay, setDueDay] = useState(''); const resource = useCrudResource<CartaoCredito, CartaoCreditoInput>({ api: cartaoCreditoApi, loadErrorMessage: t('creditCards.loadError'), createdMessage: t('creditCards.created'), updatedMessage: t('creditCards.updated'), deletedMessage: t('creditCards.deleted'), sortItems }); useEffect(() => { bandeiraCartaoApi.list().then(setBrands).catch(() => setBrandsError(t('creditCards.loadError'))); }, [t]); const reset = () => { setName(''); setCardBrandId(''); setDueDay(''); }; const submit = async (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); if (await resource.save({ name, cardBrandId, dueDay: Number(dueDay) })) reset(); }; const openEdit = (item: CartaoCredito) => { setName(item.name); setCardBrandId(item.cardBrandId); setDueDay(String(item.dueDay)); resource.openEdit(item); }; return <SimpleCrudPage section={t('creditCards.section')} title={t('creditCards.title')} newLabel={t('creditCards.new')} addLabel={t('creditCards.add')} listDescription={t('creditCards.listTitle')} columnHeader={<>{t('creditCards.columns.card')}<span className="ml-4">{t('creditCards.columns.brand')}</span><span className="ml-4">{t('creditCards.columns.dueDay')}</span></>} columnsClassName="grid-cols-[minmax(0,1fr)_minmax(0,1fr)_auto_auto]" items={resource.items} editingItem={resource.editingItem} deletingItem={resource.deletingItem} isFormOpen={resource.isFormOpen} isLoading={resource.isLoading} error={resource.error || brandsError} formError={resource.formError} emptyLabel={t('creditCards.empty')} editLabel={t('creditCards.edit')} deleteLabel={t('creditCards.deleteLabel', { name: resource.deletingItem?.name ?? '' })} deleteTitle={t('creditCards.deleteTitle')} deleteConfirmation={t('creditCards.deleteConfirmation', { name: resource.deletingItem?.name ?? '' })} onOpenCreate={() => { reset(); resource.openCreate(); }} onOpenEdit={openEdit} onCloseForm={() => { resource.closeForm(); reset(); }} onSetDeleting={resource.setDeletingItem} onRemove={resource.remove} onSubmit={submit} renderForm={() => <><div className="space-y-2"><Label htmlFor="credit-card-name" required>{t('creditCards.name')}</Label><Input id="credit-card-name" value={name} onChange={(event) => setName(event.target.value)} placeholder={t('creditCards.namePlaceholder')} maxLength={50} required autoFocus /></div><div className="space-y-2"><Label htmlFor="credit-card-brand" required>{t('creditCards.brand')}</Label><Select required value={cardBrandId || undefined} onValueChange={setCardBrandId}><SelectTrigger id="credit-card-brand"><SelectValue placeholder={t('creditCards.brandPlaceholder')} /></SelectTrigger><SelectContent>{brands.map((brand) => <SelectItem key={brand.id} value={String(brand.id)}>{brand.description}</SelectItem>)}</SelectContent></Select></div><div className="space-y-2"><Label htmlFor="credit-card-due-day" required>{t('creditCards.dueDay')}</Label><Select required value={dueDay || undefined} onValueChange={setDueDay}><SelectTrigger id="credit-card-due-day"><SelectValue placeholder={t('creditCards.dueDayPlaceholder')} /></SelectTrigger><SelectContent>{dueDays.map((day) => <SelectItem key={day} value={String(day)}>{day}</SelectItem>)}</SelectContent></Select></div></>} renderRow={(item, actions) => <div key={item.id} className="grid grid-cols-[minmax(0,1fr)_minmax(0,1fr)_auto_auto] items-center gap-3 border-b border-slate-100 px-5 py-3 text-sm text-slate-700 last:border-b-0"><span className="min-w-0 truncate font-medium">{item.name}</span><span className="min-w-0 truncate">{item.cardBrandDescription}</span><span className="whitespace-nowrap">{t('creditCards.dueDayValue', { day: item.dueDay })}</span>{actions}</div>} />; }
